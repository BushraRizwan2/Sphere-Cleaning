
import React from 'react';

/**
 * @deprecated This component is no longer used.
 * The website now functions as a single page and this dedicated blog post
 * view is no longer reachable after the removal of SPA routing.
 */
const BlogPostPage: React.FC = () => {
    return null;
};

export default BlogPostPage;
