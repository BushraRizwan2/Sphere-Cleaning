
import React from 'react';

/**
 * @deprecated This component is no longer used.
 * The website now functions as a single page and this dedicated blog index
 * page is no longer reachable after the removal of SPA routing.
 */
const BlogIndexPage: React.FC = () => {
    return null;
};

export default BlogIndexPage;
